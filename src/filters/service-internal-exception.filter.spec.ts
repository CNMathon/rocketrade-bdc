import { ServiceInternalExceptionFilter } from './service-internal-exception.filter';

describe('ServiceInternalExceptionFilter', () => {
  it('should be defined', () => {
    expect(new ServiceInternalExceptionFilter()).toBeDefined();
  });
});
