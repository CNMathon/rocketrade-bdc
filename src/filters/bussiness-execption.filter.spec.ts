import { BussinessExecptionFilter } from './bussiness-execption.filter';

describe('BussinessExecptionFilter', () => {
  it('should be defined', () => {
    expect(new BussinessExecptionFilter()).toBeDefined();
  });
});
